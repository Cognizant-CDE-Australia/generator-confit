sudo: false
language: node_js
node_js:
  - 5.4.0


before_script:
  - export CHROME_BIN=chromium-browser    # Tell Travis that we have Chrome installed (Chromium)
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  - sleep 3 # give xvfb some time to start
  - npm install --quiet -g grunt-cli bower yo
  - npm install

script:
  - npm run test:seq

notifications:
  email:
    recipients:
      - u_glow@hotmail.com
