language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ngwebapp' ]; then cd .. && eval "mv $currentfolder generator-ngwebapp" && cd generator-ngwebapp; fi
