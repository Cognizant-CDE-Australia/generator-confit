generator-confit:
  app:
    buildProfile: Latest
    projectType: browser

  buildCSS:
    sourceFormat: css

  buildJS:
    sourceFormat: TypeScript
    framework: []

  paths:
    input:
      srcDir: sourceDir/
      unitTestDir: unitTestDir/
    output:
      reportDir: reports/
    config:
      configDir: config/

  sampleApp:
    createSampleApp: true
