generator-confit:
  app:
    buildProfile: Latest
    projectType: browser

  buildCSS:
    sourceFormat: stylus

  buildJS:
    sourceFormat: ES6
    framework: []

  paths:
    input:
      srcDir: sourceDir/
      unitTestDir: unitTestDir/
    output:
      reportDir: reports/
    config:
      configDir: config/

  sampleApp:
    createSampleApp: true
